## 1. Schema and storage foundation

- [x] 1.1 Design and create `vertical_agent_templates` (versioned, append-only, mirroring `analysis_content`'s pattern: `vertical`, `version`, system-prompt additions, tool/model config, `created_at`, `UNIQUE (vertical, version)`) — `gateway/schema.sql`.
- [x] 1.2 Design and create `client_agent_instances` (`hub_id` `REFERENCES tenants(hub_id) ON DELETE CASCADE`, `vertical_template_version`, injected documentation snapshot, `version`, `created_at`) — `gateway/schema.sql`.
- [x] 1.3 Write the loader/accessor for vertical agent templates (`get_latest`/`get_version`/`list_latest`), mirroring `gateway/frameworks/store.py`'s existing pattern for `analysis_content`.
- [x] 1.4 Write tests proving vertical-template versioning: updating one vertical's template never alters another vertical's, and a prior version stays retrievable after a new one is created.

## 2. Vertical agent templates — bootstrap and content

- [x] 2.1 Bootstrap one initial `vertical_agent_templates` row per vertical (six total), derived from today's `pull_agent.py::_system_prompt` logic, so day-one behavior doesn't regress. **Run for real** against the live dev database: all 6 verticals (`saas`, `plg`, `marketplace`, `ecommerce`, `services-project`, `transactional`) ingested at version 1 with empty `system_prompt_additions`/`{}` `tool_config` — day-one system prompts are byte-identical to the prior single-tier agent's output, since `_system_prompt` only appends an additions block when non-empty.
- [x] 2.2 Decide and implement the authorship path (staff-editable directly vs. a review-ingested process) per `design.md`'s Open Question — default to staff-editable given `design.md`'s stated lean, confirmed against no objection during implementation.
- [x] 2.3 Write a test proving a vertical agent template never embeds or references client-specific data.

## 3. Client agent instantiation

- [x] 3.1 Build the production function: given `hub_id`, resolve its vertical, that vertical's current template, and its current onboarding profile, and produce a new versioned `client_agent_instances` row.
- [x] 3.2 Build the resolution function: given `hub_id`, return the current (latest) client agent instance, producing one via 3.1 if none exists yet.
- [x] 3.3 Enforce `hub_id`-scoped structural isolation on every read/write to `client_agent_instances` (a JOIN/WHERE clause, never a Python-level check a caller could skip), mirroring `gateway/frameworks/onboarding.py`'s existing pattern.
- [x] 3.4 Refuse instance production for a tenant with no known vertical set, matching `produce_onboarding_profile`'s existing refusal posture (no silent, unqualified default).
- [x] 3.5 Write isolation tests: a wrong `hub_id` cannot retrieve another client's instance; producing one client's instance never reads another tenant's data; two clients of the same vertical receive distinctly-scoped instances referencing the same vertical template version.

## 4. Rewire the pull agent to the two-tier model

- [x] 4.1 Replace `run_pull_agent(hub_id, vertical)` with `run_client_agent(hub_id)`: resolves the persisted vertical template and client instance (Sections 2-3) instead of deriving the system prompt from `analysis_content` alone.
- [x] 4.2 Preserve every isolation guarantee already proven for the single-layer version (concurrent-run isolation, mid-loop tool-call tenant binding, bounded loop, retry-freshness, audit attribution) across the new two-layer resolution step.
- [x] 4.3 Write isolation tests for the two new requirements added to `specs/vertical-pull-agent/spec.md`'s delta: a run uses the tenant's persisted instance rather than a freshly-derived one, and a run with no existing instance produces one before proceeding.
- [x] 4.4 Rebuild the Docker image, run the full test suite, confirm no regressions against the current passing count. **Run for real**: 267 passed (up from 244 pre-change), no regressions.

## 5. Custom field access — confirm before building

- [x] 5.1 Live confirmation: against a real test portal with at least one known custom property (create one if neither existing test portal has one), directly observe whether `pull_crm_objects()`'s existing `SELECT hs_object_id, * FROM {TYPE}` already returns it. Record the result the same way the `hs_object_id` precedent is recorded in `context/BLOCKERS.md`. **Run for real** against both real test portals (`148997330`, `149094230`): `SELECT *` returns only a small, fixed default set per type — 5 properties for CONTACT (`email`, `firstname`, `hs_full_name_or_email`, `hs_object_id`, `lastname`), 3 for COMPANY, 6 for DEAL — never a portal's custom properties. Confirms the gap is real; see `context/BLOCKERS.md` for the recorded finding.
- [x] 5.2 (If 5.1 confirms custom properties are already returned) Add a standard-vs-custom distinction to `tenant-field-profiling`'s output — a naming/metadata check against HubSpot's own property-definition convention, no new HubSpot access path. **N/A per 5.1's result** (gap confirmed, not "already returned") — implemented anyway as part of 5.4's mechanism, since discovery necessarily surfaces this distinction. `sync/hubspot_client.py::is_custom_property_name` — a heuristic (`hs_`-prefix reservation + a curated known-standard set), since no live-confirmed HubSpot MCP tool exposes an authoritative custom/standard flag (`search_properties`'s real response shape: `{name, label, description}` only).
- [x] 5.3 (If 5.1 confirms they are not already returned) Inspect `list_read_only_tools()`'s existing exclusion-logging buckets (`gateway/sync/hubspot_client.py`) for any already-discovered but filtered-out schema/properties-listing tool before building anything new. **Run for real**: `get_properties`/`search_properties` already exist on HubSpot's MCP surface (confirmed live via an unfiltered `list_tools()` call — 21 total tools) and were silently falling into the "read-safe but out-of-scope" bucket, the same unlogged-at-warning-level gap that previously hid `get_organization_details`.
- [x] 5.4 (If 5.1 confirms a gap and 5.3 finds nothing usable) Build an explicit custom-property discovery/pull mechanism, passed through the existing `_is_read_safe`/`_is_safe_select` checks. **Built**: `"propert"` added to `IN_SCOPE_OBJECT_KEYWORDS`; `HubSpotDataPullClient.discover_object_properties()` (wraps `search_properties`, no keywords = every property per its own documented discovery mode); `pull_crm_objects(..., properties={...})` selects explicit columns instead of `*` when given, each name validated by `_is_safe_property_name` (defense in depth, matching `_is_safe_select`'s posture) before interpolation; `frameworks/profiling.py::profile_tenant_fields` now discovers-then-pulls automatically so custom fields are actually profiled, not just theoretically reachable.
- [x] 5.5 Write a test confirming whichever mechanism results from this section stays within the existing read-only allowlist. `gateway/tests/sync/test_hubspot_client.py` — property-discovery tools are read-safe and in-scope, excluded from the generic bulk loop (`PROPERTY_DISCOVERY_TOOLS <= PER_ITEM_TOOLS`), unsafe property names rejected before SQL interpolation.
- [x] 5.6 Write an isolation test proving custom-field discovery/access, like every other multi-tenant path in this project, only ever reads the requesting tenant's own connection. `test_discover_object_properties_only_uses_this_tenants_token` — mirrors `test_pull_uses_only_this_tenants_token_never_anothers`'s existing pattern.

## 6. `query_hubspot_data` → category-scoped tools

- [x] 6.1 Define the four category groupings (CRM Records, Engagement Records, Marketing/Content, Users) and confirm every existing `CRM_OBJECT_TYPES` entry and generic-tool object type maps to exactly one category. `sync/hubspot_client.py::OBJECT_TYPE_CATEGORIES` (all 16 CRM types) + `session/live_session.py::CATEGORY_GENERIC_TOOLS` (hand-curated for the ~5 relevant generic tools, same posture as `CRM_OBJECT_TYPES` itself) — coverage asserted statically by `test_object_type_categories_cover_every_crm_object_type_exactly_once`.
- [x] 6.2 Build the four category-scoped MCP tools in `gateway/session/live_session.py` (`query_crm_records`, `query_engagement_records`, `query_marketing_content`, `query_users`), each accepting an optional `properties: list[str] | None` parameter, replacing the single `query_hubspot_data` tool's dispatch logic with a shared `_query_category` helper. An in-category-real-type-but-wrong-tool request is rejected explicitly (`{"error": ...}`), not silently returned empty.
- [x] 6.3 Wire each tool's property-selection to the calling client's onboarding-profile confirmed-relevant fields, informed by Section 5's standard-vs-custom distinction. `properties` forwards directly to `pull_crm_objects`'s explicit-column SQL. **Confirmed live** it's additive, not a strict narrowing — HubSpot's `query_crm_data` still returns its own default set alongside whatever's explicitly requested (confirmed: requesting `jobtitle`, absent from the default set, returned it populated) — docstrings corrected to say "ensures included," not "scopes/narrows."
- [x] 6.4 Retire the single generic `query_hubspot_data` tool. Removed entirely from `live_session.py`.
- [x] 6.5 Update the `tenant_pipeline_overview` and `tenant_recent_activity` prompts to reference the new tool names and appropriate object types. Now reference `query_crm_records`/`query_engagement_records` respectively.
- [x] 6.6 Write real-transport tests (matching `gateway/tests/session/test_live_session.py`'s existing `test_real_transport_*` pattern) proving each category tool works end-to-end via a real `Client`↔`FastMCP` round trip, and that an out-of-category object type is rejected rather than silently handled. **Built and passing**: `test_real_transport_lists_all_six_tools_with_correct_schemas`, `test_real_transport_query_users_round_trip`, `test_real_transport_query_crm_records_with_properties_round_trip`, `test_real_transport_query_engagement_records_rejects_out_of_category_type`, plus unit-level coverage for all four tools and the shared category-gating logic.
- [x] 6.7 Confirm live against a real test portal via MCP Inspector that the new tool surface works end-to-end, replacing the old generic tool's live confirmation. **Confirmed live** against `148997330` via a real `Client`↔`FastMCP` round trip with real (non-mocked) HubSpot pulls — only staff auth was stubbed, the same technique `test_real_transport_*` already uses: all 6 tools discovered; `query_crm_records('contacts')` returned real contact data; the `properties` parameter confirmed additive (see 6.3); `query_engagement_records('contacts')` correctly rejected as out-of-category; `query_engagement_records('calls')` and `query_users('users')` both returned real data. Full interactive MCP Inspector (browser-based Google OAuth) confirmation is a separate manual step for a human to run — not something this session can drive itself.

## 7. Documentation and tracking

- [x] 7.1 Update `CLAUDE.md`'s Architecture section to describe the two-tier vertical-template/client-instance model, replacing the "one shared agent config per vertical" language. New "Vertical/client agent separation" subsection added; the live session's tool list and prompt references updated throughout.
- [x] 7.2 Add a cross-reference note to `openspec/changes/analysis-model-templates/design.md` pointing to this change as the explicit supersession of its Non-Negotiable #6, per this change's own `design.md` Risks section. Added at both the Non-Negotiable #6 declaration and the "Shared agent per vertical" Decision, with the original text preserved for the historical record.
- [x] 7.3 Update `context/DATABASE.md` with the two new tables (`vertical_agent_templates`, `client_agent_instances`), matching this project's existing per-table documentation depth.
- [x] 7.4 Update `context/BLOCKERS.md` with the custom-field live-confirmation result (task 5.1) and any new blockers this change surfaces. Added the custom-field-access finding (mirroring the `hs_object_id` precedent's style) and the retired `query_hubspot_data`/new category-tools entry to the Resolved section; header date updated.
- [x] 7.5 Sync test counts across `CLAUDE.md`/`README.md`, matching this project's existing convention. Both updated to 287 (up from 218/244, now stale references corrected).

## 8. Out of scope for this change — not deferred, not scheduled

- [ ] 8.1 (Out of scope) The three downstream analysis jobs (Funnel, Maintenance, KPIs & Reporting) — still excluded by `HubSpot_MCP_Server_Spec_v1.2.md` Section 1.2, untouched by this change.
- [ ] 8.2 (Out of scope) The other Intelligence System inputs (Slack, ClickUp surveys, business descriptions, architecture guides, RSS) and the Architecture Guide diffing job.
- [ ] 8.3 (Out of scope) Any machine-learning training, fine-tuning, evaluation-dataset curation, or model hosting — per-client and per-vertical separation here is persisted configuration, never training.
- [ ] 8.4 (Out of scope) Per-client infrastructure, deployment, or Anthropic API credential — one shared credential and one shared deployment continue to serve every vertical and every client.
- [ ] 8.5 (Out of scope) Reformatting or rewriting Blu Mountain's authored vertical framework/Challenge Library content — still ingested verbatim into `analysis_content`; a vertical agent template layers this project's own configuration around it, never rewrites it.
