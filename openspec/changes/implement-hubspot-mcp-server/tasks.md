## 1. HubSpot OAuth (hubspot-oauth)

- [x] 1.1 Register the HubSpot Public App with the confirmed read-only scope default (full standard fields of the objects in spec Section 4.4) — done via the HubSpot CLI Developer Projects framework (Legacy public-app creation is disabled; `hs project create`/`hs project upload`); real Client ID/Secret obtained and a real install completed end to end against a test portal (`hub_id=148997330`)
- [x] 1.2 Configure the redirect URI to match `HUBSPOT_REDIRECT_URI` — `http://localhost:8888/callback`, confirmed working against the real install above
- [x] 1.3 Build the `/install` route: generate the PKCE code_verifier/code_challenge pair (43-128 chars, S256) and redirect to HubSpot's authorization URL — `gateway/hubspot_oauth.py`
- [x] 1.4 Build the `/callback` route: validate the `state` parameter before proceeding — `gateway/hubspot_oauth.py`
- [x] 1.5 Exchange the authorization code at `/oauth/v3/token`, sending credentials and code in the request body, never the URL — `gateway/hubspot_oauth.py`
- [x] 1.6 Parse and handle HubSpot's standardized RFC 6749 error fields on token exchange failure — `gateway/hubspot_oauth.py::_parse_token_error`
- [x] 1.7 Keep app credentials (Client ID/Secret) and tenant tokens strictly separate in code and storage — app credentials read from `config.settings` only, tenant tokens live only in the `tokens` table
- [x] 1.8 Decide whether `/oauth/v3/introspect` is used for token validation; document the decision either way — decided: HubSpot has no v3 introspect endpoint, `/oauth/v1/access-tokens/{token}` is used instead to resolve `hub_id`, documented in `hubspot_oauth.py`'s module docstring
- [x] 1.9 Write unit tests for PKCE generation, state validation, and error-field parsing — `gateway/tests/test_hubspot_oauth.py`, run against real Postgres in Docker, all passing

## 2. Token Vault (token-vault)

- [x] 2.1 Create the Postgres schema: a tenants table (hub_id, portal metadata, install status) and a tokens table (encrypted access token, encrypted refresh token, expiry, last refreshed) — `gateway/schema.sql`
- [x] 2.2 Implement HKDF-based per-tenant key derivation from the root key (`APP_ENCRYPTION_KEY`), using hub_id as derivation context — `gateway/crypto.py::derive_tenant_key`
- [x] 2.3 Implement AES-256 encryption/decryption using the derived per-tenant key — `gateway/crypto.py` (AES-256-GCM)
- [x] 2.4 Persist the token pair from the install flow into the vault on successful install — `gateway/hubspot_oauth.py::_persist_new_tenant`
- [x] 2.5 Implement the access-token cache (pluggable interface, TTL equal to expiry), explicitly excluding refresh tokens — `gateway/token_vault.py::AccessTokenCache`/`InMemoryAccessTokenCache`
- [x] 2.6 Implement proactive refresh: check 5 minutes ahead of expiry, refresh via the refresh token, update the vault — `gateway/token_vault.py::TokenVault.get_access_token`
- [x] 2.7 Implement Postgres advisory locks so concurrent instances never refresh the same tenant's token at once — `pg_advisory_xact_lock(hashtext(hub_id))` in `token_vault.py`
- [x] 2.8 Build the HubSpot uninstall webhook receiver: HMAC SHA-256 signature validation, reject requests older than five minutes — `gateway/token_vault.py::hubspot_uninstall_webhook`
- [x] 2.9 On a valid uninstall event, invalidate the tenant's stored token set and support re-authorization — `gateway/token_vault.py::TokenVault.invalidate`
- [x] 2.10 Write a test proving tenant A's derived key can never decrypt tenant B's stored token — `gateway/tests/test_crypto.py::test_cross_tenant_key_cannot_decrypt`, passing
- [x] 2.11 Write unit tests for the refresh-buffer logic and the cache TTL behavior — `gateway/tests/test_token_vault.py`, passing
- [x] 2.12 Write a test proving the raw HubSpot access/refresh token never appears in an Airtable write, a staff-facing response, or an audit log entry — `gateway/tests/test_hubspot_oauth.py::test_callback_accepts_valid_state_and_persists_tenant`, passing

## 3. HubSpot Data Pull (hubspot-data-pull)

- [x] 3.1 Build the per-tenant client that authenticates with the vaulted token and reads HubSpot's remote MCP endpoint — `gateway/hubspot_client.py::HubSpotDataPullClient`, connects to `mcp.hubspot.com` via FastMCP
- [x] 3.2 Implement coverage for the core object set: contacts, companies, deals, tickets, line items, products, calls, emails, meetings, notes, tasks — tool discovery + keyword filter in `hubspot_client.py`
- [x] 3.3 Implement coverage for the reference object set: users, teams, owners, campaigns/metrics, landing pages, blog posts, segments — extended to the confirmed grant's actual wording (owners, users, teams, campaigns, content, lists) in `hubspot_client.py`/`airtable_staging.py`
- [x] 3.4 Enforce least-privilege scopes at every call — `hubspot_client.py::_is_read_safe` refuses any tool name suggesting a write operation, regardless of vaulted-token scopes
- [x] 3.5 Ensure tenant context is resolved correctly on every pull, independent of what triggers it — `HubSpotDataPullClient` is constructed per hub_id, never shared across tenants
- [x] 3.6 Write a test proving a pull for one tenant can produce a complete read-only snapshot with no cross-tenant token or data leakage — `gateway/tests/test_hubspot_client.py`, using a fake MCP transport; caught and fixed a real bug in the process (see below)
- [x] 3.7 Add the MCP Auth App credential (spec Section 4.1) — confirmed live that `mcp.hubspot.com` is its own OAuth resource server and rejects the Public App's token; `auth/mcp_auth.py` (parallel install/callback flow), `mcp_tokens` table, `auth.mcp_vault` (second `TokenVault` instance); `sync/hubspot_client.py` now reads from `mcp_vault`. See design.md's decision log.
- [x] 3.8 Add coverage for the core CRM object set via `query_crm_data` — confirmed live that `mcp.hubspot.com` has no per-object tool for contacts/companies/deals/tickets/etc., only this one generic SQL-like tool; `hubspot_client.py::pull_crm_objects` authors a fixed `SELECT hs_object_id, * FROM {TYPE}` per confirmed-real object type (`CRM_OBJECT_TYPES`, discovered via `discover_hubspot_schema`), never from caller input, checked by `_is_safe_select` as defense in depth. **Corrected 2026-08-17**: originally just `SELECT * FROM {TYPE}` — confirmed live that HubSpot's default property set for every CRM object type excludes its own object ID, and "id" isn't a valid property name at all (`hs_object_id` is); every pulled CRM object's Source ID was empty as a result, so `hubspot_object_index` (used by Sybill's tenant resolution) stayed empty for every tenant, silently, with no error anywhere in the pull. Fixed by selecting `hs_object_id` explicitly and reading it from `properties.hs_object_id` in `sync/airtable_staging.py::normalize_record` (falling back to a top-level `id` for the generic per-object tools' different, separately-confirmed shape). Confirmed live against real Company/Deal records in `hub_id=149094230`. See design.md's decision log.
- [x] 3.9 Fix a real, previously-undetected bug: none of HubSpot's real MCP tools declare an `outputSchema`, so every prior tool-call result extraction (`result.data`) silently returned `None` — `hubspot_client.py::_extract_result`/`_unwrap_query_crm_data`, confirmed against real `search_owners` and `query_crm_data` response shapes. See design.md's decision log.
- [x] 3.10 Confirm the full pull end to end against a real test portal — real records returned for every core object type, matching the portal's known seed data exactly; a live token refresh observed during the run. See design.md's decision log.
- [x] 3.11 Audit every one of the 20 real discovered tools against spec Section 4.4, not just the ones already selected or already failing — found and fixed a real observability gap (`list_read_only_tools()` silently dropped read-safe-but-out-of-scope tools with no logging at all) and, through it, a real fix: `get_organization_details` (resolves the "teams" reference object) and `OBJECT_LIST`/`LANDING_PAGE`/`BLOG_POST` (resolve "segments"/"landing pages"/"blog posts", all plain `query_crm_data` object types, no action-level guard needed). See design.md's decision log.
- [x] 3.12 Fix `get_content_analytics_report` and `get_marketing_email_analytics`, both genuine missing-parameter bugs — `_DEFAULT_TOOL_PARAMS`, confirmed live returning real (valid, if empty) responses.
- [x] 3.13 Resolve `get_campaign_attribution_reports` and `read_campaign_data` — both were blocked by `CAMPAIGN`'s account-tier gate on the original test portal (`REQUIRES_ACCOUNT_MODIFICATION`), not a code gap. A second, Enterprise-tier developer test account (`hub_id=149094230`, with a real campaign created in it) confirmed both work correctly once called right: `_DEFAULT_TOOL_PARAMS` for the first (`metrics` + `hasReadToolInstructions`), a new `pull_campaign_data()` method for the second (enumerates real campaign IDs via `query_crm_data`, calls once per campaign — every operation needs a real `campaignCrmObjectId`, so no single fixed default works). Confirmed live end to end on the new account, and confirmed the original account still degrades gracefully (no crash, per-field `pull_failed`). See design.md's decision log.
- [x] 3.14 Redesign the install success/error pages from raw JSON to styled HTML — the real caller of `/callback`/`/callback/mcp-auth` is a portal admin's browser, not a programmatic consumer. `auth/pages.py`; success pages show the portal ID and, when applicable, the next step's absolute URL as a clickable link plus a button; error pages (all 7 paths across both callbacks) show a plain-language message and a retry link. Scoped to just these two routes.

## 4. Airtable Staging (airtable-staging)

- [x] 4.1 Design the Airtable base and table schema covering the full standard field set of the in-scope objects — `gateway/airtable_staging.py::OBJECT_TABLES`/`_STAGING_FIELDS`, one table per object type plus a JSON payload column so nothing is dropped
- [x] 4.2 Implement normalization logic per HubSpot object type — `gateway/airtable_staging.py::normalize_record`
- [x] 4.3 Implement the scheduled job (interval-driven) that pulls, normalizes, and writes data end to end — `gateway/airtable_staging.py::run_staging_cycle`, wired into APScheduler in `main.py`
- [x] 4.4 Tag every record written to Airtable with the owning client's identifier — every row carries `Client` = hub_id
- [x] 4.5 Verify isolation end to end: one tenant's pull can never write into another tenant's staging rows — `gateway/tests/test_airtable_staging.py::test_stage_tenant_pull_isolation_across_tenants`, `test_hubspot_object_index_isolated_per_tenant_even_with_same_object_id`
- [x] 4.6 Write an integration test that runs the scheduled job against at least one real test tenant and confirms correctly tagged, normalized output — unit-tested against a fake Airtable API and fake HubSpot pull (`gateway/tests/test_airtable_staging.py`). **Also confirmed for real, 2026-08-15**: a real staging base was created, `ensure_schema()` provisioned all 21 tables in it for real, and `run_staging_cycle()` was run manually against both real test portals (`148997330`, `149094230`) — both staged correctly, including the Enterprise-tier portal's `Campaigns`/`CampaignMetrics` and the baseline portal's graceful degradation on HubSpot's own account-tier gate for CAMPAIGN.

## 5. Sybill Ingestion (sybill-ingestion)

- [x] 5.1 Confirm Sybill's delivery mechanism (webhook vs. polling) — **Resolved: webhook**, decided 2026-08-15 on the credential actually provisioned (a live-mode `SYBILL_WEBHOOK` signing secret, no polling credential exists), not a client re-confirmation of the ambiguous earlier answer; see design.md's Risks section.
- [x] 5.2 Build the webhook receiver endpoint against the spec's stated assumption (webhook), now confirmed correct per 5.1 — `gateway/sybill_webhook.py::sybill_webhook`
- [x] 5.3 Implement HMAC SHA-256 signature validation, rejecting unsigned or stale (>5 minute) requests — `gateway/sybill_webhook.py::verify_svix_signature`, against Sybill's confirmed Svix-based signing
- [x] 5.4 Implement transcript payload normalization — `gateway/airtable_staging.py::normalize_sybill_transcript`, built against Sybill's own published schema (help.sybill.ai), not the mislabeled sample file
- [x] 5.5 Stage normalized transcripts into Airtable, tagged by client — `gateway/sybill_webhook.py::sybill_webhook`
- [x] 5.6 Write a test proving a forged or stale payload is rejected and a valid payload is staged correctly — `gateway/tests/webhooks/test_sybill.py`, passing. Tenant resolution uses `data.crm.id`/`data.crm.type` matched against `hubspot_object_index` (populated as Companies/Deals are staged); an ambiguous or unmatched payload is rejected rather than guessed at. **Corrected 2026-08-17**: the original implementation read `data.crmInfo.accountId`/`opportunityId` (two separate fields), based on a stale reading of Sybill's docs — this field never appears in Sybill's real payload (confirmed via a real "Test"-button delivery from a live Sybill trial account), so `resolve_hub_id` always raised and every real event would have been rejected. Fixed to read the real `data.crm = {id, name, type}` shape; `type` values `opportunity`/`deal` map to `deal`, `account`/`company` map to `company` (`opportunity` confirmed live, `account` inferred from the same Salesforce-flavored naming convention and not yet confirmed against a real company-linked event). Still needs validating against real Sybill traffic before go-live, and ideally a second real payload confirming the `account` mapping.

## 6. Live MCP Session (live-mcp-session)

- [x] 6.1 Register a Google Cloud OAuth 2.0 client for FastMCP's OAuth Proxy (`FASTMCP_GOOGLE_CLIENT_ID`/`FASTMCP_GOOGLE_CLIENT_SECRET`), distinct from the direct-auth path's own Google client — **Resolved 2026-08-15**: registered, and the full OAuth Proxy flow (DCR, `/authorize`, real Google consent, `/auth/callback`, token exchange, an authenticated MCP session) confirmed working end to end via a real Google login (Workspace domain `blumountain.me`) through MCP Inspector, not mocked
- [x] 6.2 Mount FastMCP's OAuth Proxy in server mode, wrapping that Google Workspace OAuth client, and confirm it serves Protected Resource Metadata and completes Dynamic Client Registration against a real MCP client (Claude Desktop, Claude Code, or Claude Cowork) — mounted at `/mcp` in `main.py`; verified the server starts and serves `/mcp` in Docker (real end-to-end connect from a real MCP client still needs 6.1's real credentials)
- [x] 6.3 Configure the proxy to issue its own audience-scoped JWT to the connecting client and confirm Google's own token never reaches that client — inherent to FastMCP's `GoogleProvider` (token factory pattern), not custom code
- [x] 6.4 Enforce the Google Workspace domain allowlist (`FASTMCP_ALLOWED_GOOGLE_DOMAINS`) on every login, not only at registration — `gateway/session/live_session.py::_require_staff_identity`
- [x] 6.5 Create the Postgres staff tenant access mechanism and the tenant-selection flow, requiring exactly one tenant selected before data returns whenever a staff member has more than one permitted tenant — revised from an allow-list to a default-open, explicitly-restrictable model (see design.md's decision log): `staff_tenant_restrictions`/`live_session_selection` tables, `session/live_session.py::_resolve_selected_tenant`
- [x] 6.6 Wire the live session's data queries through the existing `hubspot-data-pull` client and vaulted-token access, rather than a separate HubSpot client — `session/live_session.py::query_hubspot_data` uses `HubSpotDataPullClient`
- [x] 6.7 Wire per-access audit logging (staff identity, tenant, access) into the same Postgres audit log used by the scheduled staging job — `session/live_session.py` calls `security.record_audit`
- [x] 6.8 Write a test proving an unpermitted tenant query is rejected and produces no data leakage — `gateway/tests/test_tenant_isolation.py::test_live_session_restricted_tenant_rejected`, passing (renamed from `test_live_session_tenant_not_permitted_rejected` when the model moved from allow-list to default-open-with-restrictions; also added `test_live_session_default_open_access_with_no_restrictions` and `test_live_session_uninstalled_tenant_not_in_default_access` to cover the new default-open behavior explicitly)
- [x] 6.9 Write a test proving a multi-tenant staff member receives no data before explicit tenant selection — `gateway/tests/test_tenant_isolation.py::test_live_session_multi_tenant_requires_explicit_selection`, passing
- [x] 6.10 Resolve or explicitly track as open: whether Microsoft 365 needs the same OAuth Proxy treatment as Google — **decided: Google only**, not building `AzureProvider`/Microsoft Entra parity for the live session's OAuth Proxy; see design.md's Decisions section. Does not affect `session/staff_auth.py`'s own, separate Microsoft support, which is unchanged
- [x] 6.11 Resolve or explicitly track as open: whether `gateway/session/staff_auth.py` remains distinct from this session or is superseded by it — resolved: no planning document identifies a consumer for it distinct from the live session, so it's kept in the codebase but unhooked from `main.py` (`/staff/me` removed, no longer re-exported from `session/__init__.py`), in case a future non-MCP consumer (e.g. a web dashboard) needs it; see design.md's decision log

## 7. Security Hardening (security-hardening)

- [x] 7.1 Implement the Postgres-based per-tenant token-bucket rate limiter — `gateway/security.py::check_rate_limit`
- [x] 7.2 Create the Postgres audit log table (60-day minimum retention) recording every pull, credential access, and outcome — `audit_log` table in `schema.sql`, `security.py::record_audit`/`purge_expired_audit_log`
- [x] 7.3 Wire audit logging into every capability built above (hubspot-oauth, token-vault, hubspot-data-pull, airtable-staging, sybill-ingestion, live-mcp-session) — done for every capability except hubspot-data-pull's individual pulls (only the staging cycle as a whole is audited, not per-object-type pulls); tracked as a minor gap
- [x] 7.4 Sweep every code path for credential hygiene: confirm no token, secret, or key reaches a log line, prompt, or tool description — reviewed every module, plus an automated static AST scan (`gateway/tests/test_credential_hygiene.py`) that fails CI if a future logger call ever takes a credential-shaped keyword argument. Also caught and fixed a real test-isolation bug where a conftest fixture let the real `SYBILL_WEBHOOK` secret leak into test output when `.env` had a non-empty value (fixed by forcing test env vars unconditionally); that real secret should be rotated since it was displayed outside its intended boundary
- [x] 7.5 Produce a Security Controls checklist mapping each of SC-1 through SC-10 to the test that proves it — `context/Hubspot/SECURITY_CONTROLS_CHECKLIST.md`
- [x] 7.6 Write a test for every Security Control before marking it done — all 10 controls have a passing test, see the checklist above

## 8. Tenant Isolation Verification (tenant-isolation)

- [x] 8.1 Write automated tests proving no cross-tenant data or error leakage under concurrent load, across every capability above, including the live session — `gateway/tests/test_tenant_isolation.py`, passing
- [x] 8.2 Write a load test for the advisory-lock refresh path with multiple simulated instances refreshing multiple tenants concurrently — `test_tenant_isolation.py::test_concurrent_refresh_same_tenant_refreshes_once`/`test_concurrent_refresh_different_tenants_isolated`, passing
- [x] 8.3 Write a test proving one tenant's HubSpot pull or Airtable write can never write into or read from another tenant's rows under concurrent load — `gateway/tests/test_airtable_staging.py::test_stage_tenant_pull_isolation_across_tenants`, `test_hubspot_object_index_isolated_per_tenant_even_with_same_object_id`; concurrent-load version covered by `test_tenant_isolation.py`'s advisory-lock tests
- [x] 8.4 Write a test proving one staff member's live session can never read or select another staff member's permitted tenant — `test_tenant_isolation.py::test_live_session_two_staff_sessions_do_not_leak_selection`, passing
- [x] 8.5 Confirm every isolation test passes at 100%, with no flaky or skipped tests, before considering any other capability complete — all 182 tests pass (`docker compose run` against real Postgres), including every isolation, security-control, and credential-hygiene test written so far (count grows as the suite grows; kept in sync with 8.6 below rather than repeating a number that goes stale independently)
- [x] 8.6 Add the isolation test suite to re-run on every future change, not just once at launch — wired into CI at `.github/workflows/ci.yml`, running on every push/PR to `dev`/`main` via `scripts/test_all.py` (verified locally: `python3 scripts/test_all.py` passes end to end — pytest plus the live smoke test, 182/182 as of the current suite — before this was wired in, confirming the CI step actually reflects real passing behavior, not an assumption)

## 9. Deployment and Documentation

- [x] 9.1 Add a health check endpoint reflecting real service state (Postgres connectivity, scheduler status) — `gateway/main.py::health_check`, verified live: `{"status":"healthy","postgres":"connected","scheduler":"running"}`
- [ ] 9.2 Confirm all seven of the spec's own Section 10 open items are resolved before onboarding a real client — **corrected 2026-08-17 (see `openspec/changes/spec-compliance-audit/`)**: this item previously listed six things that are not the spec's actual seven Section 10 items at all (Sybill payload structure, Sybill delivery mechanism, `staff_auth.py`'s fate, Microsoft OAuth Proxy parity, HubSpot Public-vs-Private-App status, the Airtable base — real project work, just not what Section 10 enumerates). The spec's real seven items, per `context/Hubspot/IMPLEMENTATION_PLAN.md`'s own answer table (lines 146-155): (1) exact read-only scopes — confirmed; (2) portal- vs. user-level auth — confirmed portal-level; (3) compliance regime — confirmed as "none formally named, isolation is an absolute requirement regardless"; (4) expected tenant count/concurrency — confirmed, ~50 active clients; (5) secrets-management platform — **the one still genuinely open**, resolved only as a single app-level key for MVP and explicitly reopened for the real hosting environment (matches `context/BLOCKERS.md` blocker #2); (6) Sybill delivery mechanism — confirmed webhook; (7) Airtable staging-table schema — confirmed, designed and provisioned live. Six of seven resolved; #5 is blocked on the same hosting/secrets-platform decision as `BLOCKERS.md` blocker #2, not resolvable by more code.
- [x] 9.3 Write a short onboarding runbook covering how a new client portal gets connected (Flow B, HubSpot consent) — written at `context/ONBOARDING_RUNBOOK.md` (moved out of the gitignored `context/Hubspot/` directory so it's actually tracked), covering Flow B, default-open staff tenant access for the live session (Flow A), and where the direct Google/Microsoft path fits in (nowhere yet, by design)
- [ ] 9.4 Final pass confirming every FR (1-13) and SC (1-10) is actually implemented and tested, not just planned — SC-1 through SC-10 are done (Security Controls Checklist); FR-1 through FR-10 (HubSpot OAuth, token vault, data pull) confirmed against real credentials and two real test portals; FR-11 (Airtable staging) confirmed for real 2026-08-15 (see task 4.6); the live-session AF items confirmed for real 2026-08-15 too — a real Google login through MCP Inspector completed the full OAuth Proxy flow end to end. Remaining before this task can close: exercise `list_my_tenants`/`select_tenant`/`query_hubspot_data` themselves through a real client (connecting alone isn't the same as the tools being called), and item 9.2's remaining open item (real Sybill traffic).
